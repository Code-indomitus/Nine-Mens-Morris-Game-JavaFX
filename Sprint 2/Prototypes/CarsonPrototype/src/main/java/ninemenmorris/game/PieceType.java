package ninemenmorris.game;

public enum PieceType {
    /**this may screw things up. maybe we don't even need moveDir*/
    //RED(-1), BLUE(1);
    RED, BLUE;
    //final int moveDir;

    /*PieceType(int moveDir){
        this.moveDir = moveDir;
    }*/
}
